# SQL Analytics

Planned SQL areas:

- `ddl/` — analytical schemas/tables
- `transformations/` — Silver/Gold transformation SQL
- `metrics/` — governed reusable metrics and semantic views
- `analysis/` — demonstrative advanced SQL investigations

Target showcase topics include CTEs, window functions, `LAG`/`LEAD`, rolling metrics, benchmark ranking, Pareto analysis, and contribution decomposition.
